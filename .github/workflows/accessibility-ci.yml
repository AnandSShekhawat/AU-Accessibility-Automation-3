name: Automated Accessibility Scan

on:
  push:
  workflow_dispatch:

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  a11y-scan:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install axe-core CLI and static server
        run: |
          npm install -g @axe-core/cli serve
          sudo apt-get update
          sudo apt-get install -y jq

      - name: Serve site locally
        run: |
          serve . -l 3000 &
          sleep 8

      - name: Run accessibility scan
        run: |
          echo "Running accessibility scan..."
          if ! axe http://localhost:3000/before.html \
            --save a11y-report.json; then
            echo "⚠️ Axe scan failed — creating fallback report"
            echo '[]' > a11y-report.json
          fi
          echo "Scan completed"
          jq length a11y-report.json

      - name: Create GitHub issues for each violation
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          count=$(jq 'length' a11y-report.json)
          echo "Found $count accessibility violations"
          if [ "$count" -eq 0 ]; then
            echo "No accessibility issues found."
            exit 0
          fi

          jq -c '.[]' a11y-report.json | while read -r violation; do
            rule=$(echo "$violation" | jq -r '.id')
            desc=$(echo "$violation" | jq -r '.description')
            help=$(echo "$violation" | jq -r '.helpUrl')
            snippet=$(echo "$violation" | jq -r '.nodes[0].html' | sed 's/`/"/g')

            body="### Accessibility Issue: ${rule}\n\n"
            body+="**Description:**\n${desc}\n\n"
            body+="**Example element:**\n\`\`\`html\n${snippet}\n\`\`\`\n\n"
            body+="🔗 WCAG Rule: ${help}\n"
            body+="🤖 Auto-generated — Copilot can help fix this!\n"

            printf "%b\n" "$body" > issue.md

            gh issue create \
              --title "Fix: ${rule} violation" \
              --body-file issue.md \
              --label "a11y-scan" \
              --label "copilot:accessibility" || echo "Failed to create issue for ${rule}"
          done
